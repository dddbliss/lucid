<UserControl x:Class="Lucid.Controls.ItemListRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lucid.Controls"
             mc:Ignorable="d" d:DesignWidth="300" Height="80" Width="auto">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="11*"/>
        </Grid.ColumnDefinitions>
        <Label x:Name="label" Content="{Binding Item.Name}" Grid.Column="1" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Height="22" Width="auto"/>
        <Image x:Name="image" HorizontalAlignment="Left" Height="60" Margin="10,10,0,0" Grid.RowSpan="2" VerticalAlignment="Top" Width="60" Source="{Binding ImageBitmap}"/>

    </Grid>
</UserControl>
